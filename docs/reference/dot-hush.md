# Silently evaluate an R expression

Evaluates an expression while suppressing all messages, warnings, and
console output. Useful for running noisy functions without cluttering
the console.

## Usage

``` r
.hush(expr)
```

## Arguments

- expr:

  An R expression to evaluate.

## Value

The result of the evaluated expression.
