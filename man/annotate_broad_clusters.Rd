% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method-cluster_anno.R
\name{annotate_broad_clusters}
\alias{annotate_broad_clusters}
\title{Annotate clusters based on marker expression}
\usage{
annotate_broad_clusters(
  sObj,
  anno_markers,
  initial_cluster_col,
  label_out_col,
  unnassigned_lab = "Other",
  logfc.threshold = 0.1,
  min.pct = 0.1,
  only.pos = TRUE,
  p_val_adj_threshold = 0.1,
  p_adjust_method = "BH",
  ...
)
}
\arguments{
\item{sObj}{A Seurat object containing the data and existing cluster IDs.}

\item{anno_markers}{A named list of marker genes (e.g., \code{list(T_cells = c("CD3D", "CD3E"))})
or a configuration object created by \code{build_broad_marker_config}.}

\item{initial_cluster_col}{Character. The metadata column name containing the current
(usually numerical) cluster IDs.}

\item{label_out_col}{Character. The name of the new metadata column where the
biological labels will be stored.}

\item{unnassigned_lab}{Character. Label to assign if a cluster does not match any
reference markers. Default is "Other".}

\item{logfc.threshold}{Numeric. Minimum log2 fold-change required for a gene to be
considered a marker. Passed to \code{FindAllMarkers}. Default is 0.1.}

\item{min.pct}{Numeric. Minimum percentage of cells expressing the gene. Passed to \code{FindAllMarkers}.}

\item{only.pos}{Logical. If \code{TRUE}, only returns positive markers. Default is \code{TRUE}.}

\item{p_val_adj_threshold}{Numeric. Maximum adjusted p-value for a marker to be kept
before matching. Default is 0.1.}

\item{p_adjust_method}{Character. Method for p-value correction (e.g., "BH").}

\item{...}{Additional arguments passed to \code{Seurat::FindAllMarkers}.}
}
\value{
A Seurat object with a new metadata column (\code{label_out_col}) containing
the biological annotations.
}
\description{
Assigns biological labels to existing numerical clusters by running differential expression
(DE) analysis and matching the resulting markers against a provided reference list.
}
\details{
\strong{Workflow:}
\enumerate{
\item \strong{DE Analysis:} Runs \code{Seurat::FindAllMarkers} on the \code{initial_cluster_col}
to identify defining features for each numeric cluster.
\item \strong{Matching:} Filters the found markers against the \code{anno_markers} reference list.
\item \strong{Scoring:} For each cluster, calculates a score for every potential annotation type.
The score is the \strong{median avg_log2FC} of the matched markers.
\item \strong{Assignment:} Assigns the annotation with the highest score to the cluster.
Clusters with no matching markers in the reference are labelled as \code{unnassigned_lab}.
}
}
