% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-markers.R
\name{expand_markers_with_aliases}
\alias{expand_markers_with_aliases}
\title{Expand a marker reference with gene aliases}
\usage{
expand_markers_with_aliases(ref_df, alias_list, ref_marker_col = "marker")
}
\arguments{
\item{ref_df}{A data frame containing the reference markers and associated metadata.}

\item{alias_list}{A named list where names are the original markers (matching \code{ref_df})
and values are vectors of alias names.}

\item{ref_marker_col}{Character. The column name in \code{ref_df} that holds the
primary gene symbols. Default is "marker".}
}
\value{
A data frame with additional rows for the aliases. Duplicates are removed.
}
\description{
Augments a reference marker data frame by adding rows for gene aliases.
It effectively duplicates the metadata of an original marker and assigns it
to its alias, allowing the downstream scoring system to recognize both names.
}
\examples{
\dontrun{
  # Define aliases: CD8A can also be detected as CD8
  aliases <- list(CD8A = "CD8")

  # Expand the reference table
  new_ref <- expand_markers_with_aliases(reference_data, aliases)
}
}
\keyword{internal}
