% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow-wrapper.R
\name{run_cellvoteR}
\alias{run_cellvoteR}
\title{Run the complete CellVoteR ensemble pipeline}
\usage{
run_cellvoteR(seu_obj, broad_markers, fine_markers)
}
\arguments{
\item{seu_obj}{A Seurat object (normalized).}

\item{broad_markers}{A configuration list for broad cell types (from \code{\link{build_broad_marker_config}}).}

\item{fine_markers}{A named list of character vectors containing high-resolution markers.}
}
\value{
A Seurat object containing the final consensus annotations in \code{meta.data$cellvoter_label}
and the method used in \code{meta.data$cellvoter_method}. Intermediate columns from all four runs
are also preserved.
}
\description{
Executes the full automated annotation workflow by running four distinct classification
strategies in parallel and deriving a final consensus label for every cell.
}
\details{
\strong{Ensemble Strategy:}
To maximize robustness, this function runs \code{\link{run_method}} four times with different configurations:
\enumerate{
\item \strong{M1 (Base):} Clustering-based broad classification using \emph{all} features.
\item \strong{M2 (Refined):} Clustering-based broad classification using \emph{only marker} features.
\item \strong{M3 (Direct):} Marker-based broad classification using \emph{all} features.
\item \strong{M4 (Focused):} Marker-based broad classification using \emph{only marker} features.
}

\strong{Consensus & Tie-Breaking:}
\enumerate{
\item The metadata from all four runs is aggregated into a single Seurat object.
\item \code{\link{tie_breakers}} is run on the clustering outputs (M1 and M2) to generate fallback labels.
\item \code{\link{resolve_consensus_labels}} compares the four methods and assigns a final label based on majority voting.
}
}
