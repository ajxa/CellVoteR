% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score-label_cells.R
\name{assign_labels}
\alias{assign_labels}
\title{Assign new biological labels to cells}
\usage{
assign_labels(sub_sObj, sub_best_label_map, sub_sObj_col, sub_sObj_new_col)
}
\arguments{
\item{sub_sObj}{A Seurat object containing the sub-clustered cells.}

\item{sub_best_label_map}{A named character vector where names are the new labels
and values are the old cluster IDs (output of \code{get_best_label}).}

\item{sub_sObj_col}{Character. The metadata column containing the current cluster IDs (must be a factor).}

\item{sub_sObj_new_col}{Character. The name of the new metadata column to create.}
}
\value{
A Seurat object with the new annotation column added.
}
\description{
Updates the Seurat object metadata with new cell type labels based on the mapping
created by \code{\link{get_best_label}}.
}
\details{
This function handles two scenarios:
\itemize{
\item \strong{Single Cluster:} Directly assigns the single label to the new column.
\item \strong{Multiple Clusters:} Uses \code{forcats::fct_recode} to map old cluster IDs
to new text labels.
}
}
