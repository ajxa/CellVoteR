% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-markers.R
\name{clean_markers}
\alias{clean_markers}
\title{Clean and split broad and fine markers}
\usage{
clean_markers(
  fine_marker_df,
  broad_marker_df,
  fine_marker_col = "marker",
  fine_label_col = "fine_label",
  fine_category_col = "fine_category",
  broad_category_col = "broad_category",
  broad_marker_col = "marker",
  unnamed_broad_cat_label = "other"
)
}
\arguments{
\item{fine_marker_df}{A dataframe containing specific cell markers and their labels.}

\item{broad_marker_df}{A dataframe containing higher-level categories to validate against.}

\item{fine_marker_col}{String. The column name for markers in \code{fine_marker_df}. Defaults to "marker".}

\item{fine_label_col}{String. The column name for the specific cell type label (e.g., "B cell") in \code{fine_marker_df}. Defaults to "fine_label".}

\item{fine_category_col}{String. The column name for the broader category (e.g., "immune") in \code{fine_marker_df}. Defaults to "fine_category".}

\item{broad_category_col}{String. The column name for categories in \code{broad_marker_df}. Defaults to "broad_category".}

\item{broad_marker_col}{String. The column name for markers in \code{broad_marker_df}. Defaults to "marker".}

\item{unnamed_broad_cat_label}{String. The label to assign to fine categories that do not match any broad category. Defaults to "other".}
}
\value{
A named list containing three elements:
\itemize{
\item \strong{fine}: A nested list where top-level keys are \code{fine_category} and sub-keys are \code{fine_label}. Values are character vectors of markers.
\item \strong{broad}: A named list where keys are \code{broad_category} and values are character vectors of markers.
\item \strong{all}: A single character vector containing every unique marker found in both the fine and broad datasets.
}
}
\description{
This function validates that all broad categories exist within the fine marker dataframe.
It reassigns unmatched fine categories to a generic label, splits both datasets into
hierarchical lists, and compiles a comprehensive vector of all unique markers found
across both datasets.
}
\examples{
# res <- clean_markers(fine_df, broad_df)
# head(res$all) # View all unique markers

}
