% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method-cluster_sub.R
\name{supplement_markers}
\alias{supplement_markers}
\title{Supplement insufficient DE Markers}
\usage{
supplement_markers(de_results, avg_expr, cl, target_n = 100)
}
\arguments{
\item{de_results}{Data frame. The existing DE results (output of \code{FindAllMarkers}).}

\item{avg_expr}{Matrix. Average expression matrix (genes x clusters).}

\item{cl}{Character or Numeric. The cluster ID to supplement.}

\item{target_n}{Integer. The desired total number of markers (existing + supplemental).}
}
\value{
A data frame of supplemental markers formatted to match \code{de_results}.
}
\description{
Adds highly expressed genes to a cluster's marker list if the Differential Expression (DE)
analysis yields too few results. This ensures downstream enrichment tests do not fail due
to empty input sets.
}
\details{
The function identifies the top expressed genes in the specific cluster (based on \code{avg_expr})
that are not already present in \code{de_results}. It creates a "fallback" data frame where
statistical columns (p_val, p_val_adj) are set to 1, and \code{is_supplemental} is flagged as \code{TRUE}.
}
