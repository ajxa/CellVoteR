% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/method-cluster.R
\name{set_cluster_params}
\alias{set_cluster_params}
\title{Calculate dynamic clustering parameters}
\usage{
set_cluster_params(
  n_cells,
  mins_cluster_cells = 500,
  res_min = 0.6,
  res_max = 2,
  res_saturation_n = 1e+05,
  npc_min = 20,
  npc_max = 50,
  npc_slope = 4,
  subcluster = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{n_cells}{Integer. The number of cells in the dataset/subset.}

\item{mins_cluster_cells}{Integer. Minimum number of cells required to perform clustering.
If \code{n_cells} is below this, returns \code{skip = TRUE}.}

\item{res_min}{Numeric. Minimum resolution value.}

\item{res_max}{Numeric. Maximum resolution value.}

\item{res_saturation_n}{Integer. The cell count at which resolution reaches \code{res_max}.}

\item{npc_min}{Integer. Minimum number of PCs to use.}

\item{npc_max}{Integer. Maximum number of PCs to use.}

\item{npc_slope}{Numeric. Slope factor for the logarithmic calculation of PCs.}

\item{subcluster}{Logical. Controls the verbosity label.}

\item{verbose}{Logical. If \code{TRUE}, prints the calculated parameters to the console.}
}
\value{
A list containing:
\itemize{
\item \code{npcs}: Integer. Number of PCs to use.
\item \code{dims}: Integer vector. Sequence of dimensions (e.g., \code{1:30}).
\item \code{res}: Numeric. The calculated resolution.
\item \code{skip}: Logical. Whether to skip clustering (TRUE if cells < \code{mins_cluster_cells}).
}
}
\description{
Determines the optimal resolution and number of principal components (PCs)
based on the number of cells in the dataset.
}
\details{
This function scales parameters dynamically to avoid over-clustering small datasets
or under-clustering large ones:
\itemize{
\item \strong{Resolution:} Scales linearly between \code{res_min} and \code{res_max}
based on the square root of \code{n_cells}.
\item \strong{PCs:} Scales logarithmically based on \code{n_cells}.
}
If \code{n_cells} is less than \code{mins_cluster_cells}, clustering is flagged to be skipped.
}
